// import Auth from 'auth';
// import { expect } from 'chai';

describe('Authentication', () => {
  describe('login', () => {
    it('makes a call to google API passing it the key, secret, and redirect URI');
  });

  describe('auth');
});
